/*
const { getEnv, setGlobal } = require('guld-env')
const { getFS } = require('guld-fs')
const { getConfig, setConfig } = require('guld-config')
const path = require('path')
const home = require('user-home')
var fs

module.exports = {
}
*/
